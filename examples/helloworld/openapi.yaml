openapi: 3.0.3
info:
  title: Apigee Forge Hello World
  version: 1.0.0
  description: Minimal API used to validate the non-interactive CLI pipeline.
servers:
  - url: https://mocktarget.apigee.net
paths:
  /hello:
    get:
      operationId: hello
      responses:
        '200':
          description: Hello World response
          content:
            text/plain:
              schema:
                type: string
